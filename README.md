# 🎄 Мафия: Новогодний Переполох - Информационный сайт

Простой информационный сайт с правилами игры и последними обновлениями.

## 📁 Структура проекта

```
suit/
├── flask_app.py          # Основной Flask приложение
├── requirements.txt       # Зависимости Python
├── templates/            # HTML шаблоны
│   ├── base.html        # Базовый шаблон
│   ├── index.html       # Главная страница
│   ├── rules.html       # Правила игры
│   ├── updates.html     # Последние обновления
│   ├── about.html       # О проекте
│   └── static/          # Статические файлы
│       ├── css/
│       │   └── style.css
│       └── js/
│           └── main.js
└── README.md            # Этот файл
```

## 🚀 Установка и запуск

### Локально

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
python flask_app.py
```

3. Откройте в браузере: `http://localhost:5000`

### На PythonAnywhere

1. Загрузите файлы в директорию `mysite/`

2. Установите зависимости в Bash консоли:
```bash
pip3.10 install --user -r requirements.txt
```

3. В разделе **Web** → **WSGI configuration file** укажите:
```python
import sys
path = '/home/YourUsername/mysite'
if path not in sys.path:
    sys.path.append(path)

from flask_app import app as application
```

4. Перезагрузите приложение в разделе **Web** → **Reload**

## 📄 Страницы сайта

- **/** - Главная страница с кратким описанием
- **/rules** - Подробные правила игры
- **/updates** - Последние обновления проекта
- **/about** - Информация о проекте

## 🎨 Особенности

- Простой и чистый дизайн
- Адаптивная верстка (Tailwind CSS)
- Новогодняя тематика
- Информационный контент без API интеграций

## 📝 Зависимости

- `flask>=1.0.0` - Веб-фреймворк

## 🔧 Настройка

Все настройки находятся в `flask_app.py`. Для изменения контента редактируйте HTML шаблоны в `templates/`.
